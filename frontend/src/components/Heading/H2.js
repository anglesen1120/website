/**
 *
 * H2
 *
 */

import styled from 'styled-components';

const H2 = styled.h2`
  font-size: 3em;
  color: #fff;
  font-family: 'Aovel Neo';
  font-weight: 700;
  letter-spacing: -0.5px;
`;

export default H2;
